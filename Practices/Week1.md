🧪 DAY 1 — VARIABLES + DATA TYPES
🎯 Problem

Create variables for:

driver name
truck plate number
fuel price per liter
availability (true/false)

👉 Print:

Driver [name] with truck [plate] is [available/not available]

🧪 DAY 2 — OPERATORS + CONDITIONS
🎯 Problem

Given:

let budget = 8000;
let fuelCost = 9500;

👉 Check:

If fuelCost is greater than budget → print "Over Budget"
Otherwise → print "Within Budget"
🧪 DAY 3 — FUNCTIONS
🎯 Problem

Create a function:

calculateDeliveryCost(distance, rate)

👉 It should return total delivery cost

👉 Call the function and print result

🧪 DAY 4 — ARRAYS
🎯 Problem

Given:

let trucks = ["ABC123", "XYZ789", "DEF456"];

👉 Do the following:

Print all trucks
Add a new truck
Remove the first truck
Print updated list
🧪 DAY 5 — OBJECTS
🎯 Problem

Create an object:

booking

With:

id
driver name
status

👉 Do:

Print driver name
Change status to "completed"
Print updated object
🧪 DAY 6 — MINI PROBLEM
🎯 Problem

Given:

let bookings = [
{ id: 1, driver: "Jessie" },
{ id: 2, driver: "Mark" }
];

👉 Print:

Booking 1 handled by Jessie
Booking 2 handled by Mark
🧪 DAY 7 — REVIEW TASK
🎯 Problem

Explain in code:

difference between array and object
create one example of each
