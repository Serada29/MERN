🚛 COMBO 1 — DRIVER BOOKINGS
🎯 Problem

Given:

let drivers = ["Jessie", "Mark"];

let bookings = [
{ id: 1, driver: "Jessie", status: "pending" },
{ id: 2, driver: "Mark", status: "completed" }
];

👉 For each booking:

Print booking id
Print driver name
Print status
🚛 COMBO 2 — COST FILTER
🎯 Problem

Given:

let bookings = [
{ distance: 100, rate: 10 },
{ distance: 200, rate: 5 }
];

👉 Tasks:

Create a function to compute cost
Create a new array with cost > 800
Print results
🚛 COMBO 3 — FULL BOOKING SYSTEM
🎯 Problem

Given:

let bookings = [
{
id: 1,
truck: { plate: "ABC123" },
distance: 100,
rate: 10,
fuel: { liters: 20 },
status: "pending",
date: new Date()
}
];

👉 For each booking:

Compute:
delivery cost
fuel cost (assume fuel price = 65)
profit
Print:
Truck [plate]
Profit: [value]
Status: [High / Normal / Loss]
Date: [formatted]
🚛 COMBO 4 — SUMMARY DASHBOARD
🎯 Problem

Given multiple bookings

👉 Compute:

total profit
total pending bookings
total completed bookings
🧠 FINAL RULE

👉 Solve everything using:

variables
functions
loops
conditions
arrays
objects
