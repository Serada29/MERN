🧪 DAY 1 — LOOPS
🎯 Problem

Given:

let bookings = [
{ id: 1 },
{ id: 2 },
{ id: 3 }
];

👉 Loop through and print all booking IDs

🧪 DAY 2 — ARRAY METHODS
🎯 Problem

Given:

let bookings = [
{ id: 1, status: "pending" },
{ id: 2, status: "completed" },
{ id: 3, status: "pending" }
];

👉 Create a new array that contains only "pending" bookings

🧪 DAY 3 — NESTED OBJECTS
🎯 Problem

Given:

let booking = {
id: 1,
driver: {
name: "Jessie",
license: "A123"
},
truck: {
plate: "ABC123"
}
};

👉 Print:

driver name
truck plate
🧪 DAY 4 — FUNCTIONS WITH PARAMETERS
🎯 Problem

Create a function:

calculateFuelCost(liters, pricePerLiter)

👉 Return total fuel cost

🧪 DAY 5 — DATES
🎯 Problem

Create a variable with current date

👉 Print in format:

YYYY-MM-DD
🧪 DAY 6 — SIMPLE LOGIC
🎯 Problem

Given:

let profit = 1200;

👉 Print:

"High Profit" if > 1000
"Loss" if < 0
"Normal" otherwise
🧪 DAY 7 — MINI PROBLEM
🎯 Problem

Given:

let bookings = [
{ distance: 100, rate: 10 },
{ distance: 50, rate: 20 }
];

👉 Compute and print total cost for each booking
